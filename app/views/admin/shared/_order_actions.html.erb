<%= link_to_if order.address?, "Отметить", fire_admin_order_path(:id => order, :e => :next), :method => :put  do %>
<% end %>
<br/>
<%= link_to_if order.complete? && current_user.has_role?(:admin), "Выгрузить", export_path(:id => order), :method => :put do %>
<%end%>

<%= link_to_if order.complete? && current_user.has_role?(:admin), "Удалить", fire_admin_order_path(:id => order, :e => :next), :method => :put do %>
<% end %>
